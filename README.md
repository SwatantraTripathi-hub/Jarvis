# Jarvis

A full-stack AI assistant project with a Node.js backend and React frontend.

## Structure

- `Backend/` — API, auth, chat, socket, and AI services
- `frontend/` — React client for the Jarvis interface

## Getting started

Install dependencies and run the backend/frontend from their respective folders.
